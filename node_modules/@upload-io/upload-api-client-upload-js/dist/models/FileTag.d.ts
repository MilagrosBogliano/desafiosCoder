import type { FileTagName } from "./FileTagName";
export declare type FileTag = {
    name: FileTagName;
    /**
     * Specifies whether the tag is searchable from the Upload Dashboard. File are allowed up to 10 searchable tags.
     */
    searchable: boolean;
};
