import type { FilePartCount } from "./FilePartCount";
import type { FileSummary } from "./FileSummary";
import type { TransformationUrlSlug } from "./TransformationUrlSlug";
import type { UploadPart } from "./UploadPart";
/**
 * File upload initialization response.
 */
export declare type BeginUploadResponse = {
    file: FileSummary;
    suggestedOptimizationUrlSlug?: TransformationUrlSlug;
    uploadParts: {
        first: UploadPart;
        count: FilePartCount;
    };
};
