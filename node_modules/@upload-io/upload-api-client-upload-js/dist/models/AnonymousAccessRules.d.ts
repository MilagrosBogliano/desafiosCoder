/**
 * Settings that dictate whether users must be authenticated to perform uploads and/or downloads.
 */
export declare type AnonymousAccessRules = {
    /**
     * If true, anonymous visitors from the Internet can download any file from the account (assuming they have a file ID).
     */
    allowUnauthenticatedDownloads: boolean;
    /**
     * If true, anonymous visitors from the Internet can upload new files to the account.
     */
    allowUnauthenticatedUploads: boolean;
};
