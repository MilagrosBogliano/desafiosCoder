import type { PublicKeyName } from "./PublicKeyName";
import type { PublicKeyPEM } from "./PublicKeyPEM";
export declare type PublicKeyData = {
    certificate: PublicKeyPEM;
    name: PublicKeyName;
};
