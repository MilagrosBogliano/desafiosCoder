/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { ContinuationToken } from "./ContinuationToken";

export type ContinuationTokens = {
  /**
   * Cursor to the previous page of results. Null if currently on the first page.
   */
  backwardToken: ContinuationToken | null;
  /**
   * Cursor to the next page of results. Null if currently on the last page.
   */
  forwardToken: ContinuationToken | null;
};
