import { Uploader } from "..";
export declare class InlineWidgetBinder {
    private readonly uploader;
    private readonly elementTracker;
    private readonly attributes;
    constructor(uploader: Uploader);
    bindWidgetsAndMonitor(): void;
    private bindWidget;
    private bindInlineWidget;
    private bindGenericWidget;
    private openUploader;
    private fireUploadComplete;
    private getConfig;
}
