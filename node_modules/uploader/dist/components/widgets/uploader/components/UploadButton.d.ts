import { JSX } from "preact";
export declare const UploadButton: ({ className, multi, onUpload, text }: {
    className?: string | undefined;
    multi: boolean;
    onUpload: (files: File[]) => void;
    text: string;
}) => JSX.Element;
