import { JSX } from "preact";
import { Upload, UploadedFile } from "upload-js";
import { UploaderLocale } from "../../../../..";
import "./ImageCropper.scss";
interface Props {
    circular: boolean;
    locale: UploaderLocale;
    multi: undefined | {
        imageCount: number;
        imageIndex: number;
    };
    onFinish: (editedFile: UploadedFile | undefined) => void;
    originalImage: UploadedFile;
    ratio: number | undefined;
    upload: Upload;
}
export declare const ImageCropper: ({ locale, originalImage, upload, onFinish, ratio, multi, circular }: Props) => JSX.Element;
export {};
