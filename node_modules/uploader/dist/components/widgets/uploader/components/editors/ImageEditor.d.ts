import { JSX } from "preact";
import { Upload, UploadedFile } from "upload-js";
import { UploaderLocale } from "../../../../..";
interface Props {
    cropCircular: boolean;
    cropRatio: number | undefined;
    locale: UploaderLocale;
    multi: undefined | {
        imageCount: number;
        imageIndex: number;
    };
    onImageEdited: (editedFile: UploadedFile | undefined) => void;
    originalImage: UploadedFile;
    upload: Upload;
}
export declare const ImageEditor: ({ originalImage, upload, onImageEdited, locale, cropRatio, multi, cropCircular }: Props) => JSX.Element;
export {};
