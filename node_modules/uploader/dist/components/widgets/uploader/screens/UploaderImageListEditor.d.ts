import { JSX } from "preact";
import { Upload, UploadedFile } from "upload-js";
import { UploadedFileContainer } from "../../../../components/widgets/uploader/model/SubmittedFile";
import { UploaderLocale } from "../../../..";
interface Props {
    cropCircular: boolean;
    cropRatio: number | undefined;
    images: UploadedFileContainer[];
    locale: UploaderLocale;
    multi: boolean;
    onImageEdited: (editedFile: UploadedFile | undefined, sparseFileIndex: number) => void;
    upload: Upload;
}
export declare const UploaderImageListEditor: ({ images, onImageEdited, upload, locale, cropCircular, cropRatio, multi }: Props) => JSX.Element;
export {};
