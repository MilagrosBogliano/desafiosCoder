import { JSX } from "preact";
import { UploaderParamsRequired } from "../../../../UploaderParams";
interface Props {
    addFiles: (files: File[]) => void;
    isImageUploader: boolean;
    params: UploaderParamsRequired;
}
export declare const UploaderWelcomeScreen: ({ addFiles, params: { multi, locale }, isImageUploader }: Props) => JSX.Element;
export {};
