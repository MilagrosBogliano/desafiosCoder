import { JSX } from "preact";
import { SubmittedFile, UploadedFileContainer } from "../../../../components/widgets/uploader/model/SubmittedFile";
import { UploaderParamsRequired } from "../../../../UploaderParams";
import "./UploaderMainScreen.scss";
interface Props {
    addFiles: (files: File[]) => void;
    finalize: () => void;
    isImageUploader: boolean;
    params: UploaderParamsRequired;
    remove: (fileIndex: number) => void;
    submittedFiles: SubmittedFile[];
    uploadedFiles: UploadedFileContainer[];
}
export declare const UploaderMainScreen: ({ addFiles, submittedFiles, uploadedFiles, params, remove, finalize, isImageUploader }: Props) => JSX.Element;
export {};
