import { ReactNode } from "../../../modules/common/React";
import { JSX } from "preact";
import "./WidgetBase.scss";
import { UploaderLayout } from "../../../UploaderLayout";
interface Props {
    children: ReactNode;
    htmlProps?: any;
    isDraggable?: boolean;
    isDragging?: boolean;
    layout: UploaderLayout;
    multi: boolean;
}
export declare const WidgetBase: ({ children, htmlProps, isDraggable, isDragging, layout, multi }: Props) => JSX.Element;
export {};
